import 'package:flutter/material.dart';

const colorFontPallets = [
  Color.fromRGBO(26, 31, 56, 1),
  Color.fromRGBO(72, 76, 99, 1),
  Color.fromRGBO(149, 149, 163, 1),
];

const colorPrimary = Colors.blue;
const colorSuccess = Colors.teal;
const colorInfo = Colors.lightBlue;
const colorWarning = Colors.orange;
const colorDanger = Colors.red;
